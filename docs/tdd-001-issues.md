# TDD-001 实施中发现的问题

实现时发现规格歧义 / 错误 / 缺口，记在这里，等人工裁定后修 Outline 上的 TDD-001。不要在代码里绕过。

「当前实现」列是等待裁定期间采取的保守选择，代码中以 `issues #n` 注释标记；裁定若不同，按裁定改码并清除标记。**2026-08-27 用户口头裁定一批（见「裁定」列），引擎已按裁定改码；TDD-001 与规则书的对应段落尚未回写，待更新 Outline / rulebook 后清账。**

| # | 日期 | TDD 位置 | 问题 | 当前实现 | 裁定 |
|---|---|---|---|---|---|
| 1 | 2026-08-27 | §4.1 / §6.2 步骤 10 / §6.4 步骤 a | §6.2 与 §6.4 引用 `pendingQualifications`，但 §4.1 的 `Game` 接口没有声明该字段，结构未定义 | 在 `Game` 中补 `pendingQualifications: PendingQualification[]` | |
| 2 | 2026-08-27 | §4.2 | `entry: QualificationRequirement` 类型从未定义；高级资格能否顶替低级（规则书 §5.4「核心资格可以满足一般基础准入」）未形式化 | 封闭表示 `NONE / AT_LEAST_BASIC / ANY_OF / CORE_OR_TWO_ORG`；列举严格按卡面认定，无任何高低替代；「基础以上」= 持任意资格 | **2026-08-27 用户：高级资格与低级资格不能混用，严格按列举认定。已实现；规则书 §5.4 末段（核心可满足一般基础准入）需回写删除** |
| 3 | 2026-08-27 | §6.2 步骤 6 | 「取整余数给队长（teamId 创建者）」——`Submission` 模型（§4.3）不含创建者信息，引擎不可观测 | 已由 #12 收款人机制取代，无队长概念 | **2026-08-27 用户：按比例分配整体废除，见 #12** |
| 4 | 2026-08-27 | §6.2 步骤 4 / 规则书 §14.1 | 「资格优先」档位 2「核心资格 / 项目接受的其他高级资格」：entry 为「无」或「基础以上」时高级资格算不算？ | 高级档（核心/组织/工程）仅当 entry 明确列举时成立；门槛无资格限制的项目不存在高级档 | **2026-08-27 用户：门槛没有资格限制就不应有高级档一说。已实现；规则书 §14.1 需回写明确** |
| 5 | 2026-08-27 | §4.3 / §6.2 步骤 1 | 工程队每名成员各提交一个 `bid`，bid 不一致时如何处理未规定 | bid 不一致 → 队伍作废（按落选处理） | **2026-08-27 用户（对收款人裁定的同理适用）：「达不成统一意见就没法竞标，自动算失败」——维持作废** |
| 6 | 2026-08-27 | §10.1 | 校验表未覆盖输入卫生：负数/非整数数值、members 不含提交者本人、members 含重复 | 数值畸形 → 拒绝整份提交；members 畸形 → 剔除该 entry | |
| 7 | 2026-08-27 | §6.2 步骤 2 | 战争合法队过滤条件未包含人数检查，但 §4.2 卡面携带 `teamSize`、规则书 §11.3 有人数列 | 战争按 `teamSize` 过滤，人数越界按落选处理；工程/商业无人数列，不检查 | |
| 8 | 2026-08-27 | §6.2 步骤 10 / 规则书 §7.2–7.4 | 同时攒到两条晋升路（如 2 工程 + 2 行政记录）时升一项还是全升、优先级未定 | 只排队一项，按规则书章节顺序取先：工程 → 行政 → 组织 | |
| 9 | 2026-08-27 | §6.2 步骤 2 / 步骤 6 | 落选退款 `floor(funds×0.8)` 是队级公式，按成员分配的舍入规则未定 | 逐成员独立取 `floor(0.8 × 该成员投入)` | |
| 10 | 2026-08-27 | §10.1 vs §6.2 步骤 10 / 规则书 §7.1 | 基础资格购买的 stamps ≥ 2 校验时点：提交时查则本回合新印章不计，最早第 4 回合取得；规则书说最早第 3 回合 | 印章在结算步骤 10 点数（本回合新得的算数）；不足则退还锁定的 20。与规则书「最早第 3 回合」一致 | **2026-08-27 用户授权引擎自定合理方案。已实现；TDD §10.1 该行需回写** |
| 11 | 2026-08-27 | §6.2 步骤 10 / §6.4 边界 | 第 6 回合购买资格：消耗 20 但资格永不生效 | 第 6 回合直接拒绝该字段（不锁定、不扣款） | **2026-08-27 用户授权引擎自定（“最后一回合买资格没什么意义”）。已实现** |
| 12 | 2026-08-27 | §4.3 / §6.2 步骤 6 / 附录 C.1 | 队伍收益如何分配（TDD 原文：按 ability 比例、余数给队长；附录 C.1 待验证） | 新机制：团队 entry 增加 `payee` 字段（须为 members 之一），全员一致指定，不一致 → 队伍作废；中标收益**全额**在下一回合开始时打给 payee（第 6 回合中标则终局前即时到账），队内分配走自由转账，不分是内部信用问题（阶段 2 可用备忘契约指控）。**副作用：本回合危机处罚打不到尚未到账的收益。**行政为个人项目，报酬仍当回合入账 | **2026-08-27 用户：组队提交须注明钱打给谁，达不成一致算失败；成功后钱下一回合打给被指定结算人；他不分出去算他们内部违约。已实现；TDD §4.3/§6.2/附录 C.1 需回写** |
| 13 | 2026-08-27 | 规则书 §11.3 准入列 | （裁定引发的规则变更）战争项目原有准入（基础以上 / 组织·核心 / 核心或 2 组织） | 六张战争卡 entry 一律改为无准入；资格加成（§11.1 基础+0/组织+30/核心+60，上限 60）保留 | **2026-08-27 用户：打仗需要人力，人人可参战，不应设准入。已实现；规则书 §11.3 准入列需回写** |
