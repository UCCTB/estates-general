# TDD-001 实施中发现的问题

实现时发现规格歧义 / 错误 / 缺口，记在这里，等人工裁定后修 Outline 上的 TDD-001。不要在代码里绕过。

「当前实现」列是等待裁定期间采取的保守选择，代码中以 `issues #n` 注释标记；裁定若不同，按裁定改码并清除标记。
**2026-08-27 结账：已裁定条目（#2 #3 #4 #5 #10 #11 #12 #13）已全部回写，引擎实现与文档一致。**

- TDD-001 升至 **v0.2**（Outline）：§4.1 补齐 `Game` 缺失字段、§4.2 补 `QualificationRequirement` 并改为严格按卡面列举、§4.3 增 `payee` 与成队四项一致条件、§6.2 步骤 2 / 4 / 6 / 10 重写、§6.4 增 a0 在途收益到账、§8.1 增可见性表、§10.1 同步校验表、附录 B 全部标 ✅、附录 C.1 结案。
- 规则书升至 **V1.0.1**（`docs/rulebook-v1.md`）：§3 数字版组件、§5.4 资格严格列举、§5.5 信用增两条、§7.1 印章清点时点与第 6 回合禁购、§9 情报窗口 / 提交增列收款人 / 结算三段 / 新增「第四阶段附则：队伍收益的支付」、§10 新增 10.1–10.3、§11.3 战争取消准入、§14.1 资格优先档位 2、§17.1 成就三档。
- **未裁定条目**（#6 #7 #8 #9 #14–#19）已在 TDD-001 **附录 E** 立项，编号对照：E.1←#6、E.2←#14、E.3←#17、E.4←#19、E.5←#8。#1 属 TDD 结构缺漏（字段被引用却未声明），随 v0.2 直接补齐，不再作为待裁定项。#7 #9 #15 #16 #18 的当前实现已写入 TDD 正文（§6.2 步骤 2、§5.6 末段、§8.1 可见性表、§5.2 语义说明），若与设计意图不符，裁定后回改。

| # | 日期 | TDD 位置 | 问题 | 当前实现 | 裁定 |
|---|---|---|---|---|---|
| 1 | 2026-08-27 | §4.1 / §6.2 步骤 10 / §6.4 步骤 a | §6.2 与 §6.4 引用 `pendingQualifications`，但 §4.1 的 `Game` 接口没有声明该字段，结构未定义 | 在 `Game` 中补 `pendingQualifications: PendingQualification[]` | |
| 2 | 2026-08-27 | §4.2 | `entry: QualificationRequirement` 类型从未定义；高级资格能否顶替低级（规则书 §5.4「核心资格可以满足一般基础准入」）未形式化 | 封闭表示 `NONE / AT_LEAST_BASIC / ANY_OF / CORE_OR_TWO_ORG`；列举严格按卡面认定，无任何高低替代；「基础以上」= 持任意资格 | **2026-08-27 用户：高级资格与低级资格不能混用，严格按列举认定。已实现；规则书 §5.4 末段（核心可满足一般基础准入）需回写删除** （已回写 TDD v0.2 / 规则书 V1.0.1）|
| 3 | 2026-08-27 | §6.2 步骤 6 | 「取整余数给队长（teamId 创建者）」——`Submission` 模型（§4.3）不含创建者信息，引擎不可观测 | 已由 #12 收款人机制取代，无队长概念 | **2026-08-27 用户：按比例分配整体废除，见 #12** （已回写 TDD v0.2 / 规则书 V1.0.1）|
| 4 | 2026-08-27 | §6.2 步骤 4 / 规则书 §14.1 | 「资格优先」档位 2「核心资格 / 项目接受的其他高级资格」：entry 为「无」或「基础以上」时高级资格算不算？ | 高级档（核心/组织/工程）仅当 entry 明确列举时成立；门槛无资格限制的项目不存在高级档 | **2026-08-27 用户：门槛没有资格限制就不应有高级档一说。已实现；规则书 §14.1 需回写明确** （已回写 TDD v0.2 / 规则书 V1.0.1）|
| 5 | 2026-08-27 | §4.3 / §6.2 步骤 1 | 工程队每名成员各提交一个 `bid`，bid 不一致时如何处理未规定 | bid 不一致 → 队伍作废（按落选处理） | **2026-08-27 用户（对收款人裁定的同理适用）：「达不成统一意见就没法竞标，自动算失败」——维持作废** （已回写 TDD v0.2 / 规则书 V1.0.1）|
| 6 | 2026-08-27 | §10.1 | 校验表未覆盖输入卫生：负数/非整数数值、members 不含提交者本人、members 含重复 | 数值畸形 → 拒绝整份提交；members 畸形 → 剔除该 entry | |
| 7 | 2026-08-27 | §6.2 步骤 2 | 战争合法队过滤条件未包含人数检查，但 §4.2 卡面携带 `teamSize`、规则书 §11.3 有人数列 | 战争按 `teamSize` 过滤，人数越界按落选处理；工程/商业无人数列，不检查 | |
| 8 | 2026-08-27 | §6.2 步骤 10 / 规则书 §7.2–7.4 | 同时攒到两条晋升路（如 2 工程 + 2 行政记录）时升一项还是全升、优先级未定 | 只排队一项，按规则书章节顺序取先：工程 → 行政 → 组织 | |
| 9 | 2026-08-27 | §6.2 步骤 2 / 步骤 6 | 落选退款 `floor(funds×0.8)` 是队级公式，按成员分配的舍入规则未定 | 逐成员独立取 `floor(0.8 × 该成员投入)` | |
| 10 | 2026-08-27 | §10.1 vs §6.2 步骤 10 / 规则书 §7.1 | 基础资格购买的 stamps ≥ 2 校验时点：提交时查则本回合新印章不计，最早第 4 回合取得；规则书说最早第 3 回合 | 印章在结算步骤 10 点数（本回合新得的算数）；不足则退还锁定的 20。与规则书「最早第 3 回合」一致 | **2026-08-27 用户授权引擎自定合理方案。已实现；TDD §10.1 该行需回写** （已回写 TDD v0.2 / 规则书 V1.0.1）|
| 11 | 2026-08-27 | §6.2 步骤 10 / §6.4 边界 | 第 6 回合购买资格：消耗 20 但资格永不生效 | 第 6 回合直接拒绝该字段（不锁定、不扣款） | **2026-08-27 用户授权引擎自定（“最后一回合买资格没什么意义”）。已实现** （已回写 TDD v0.2 / 规则书 V1.0.1）|
| 12 | 2026-08-27 | §4.3 / §6.2 步骤 6 / 附录 C.1 | 队伍收益如何分配（TDD 原文：按 ability 比例、余数给队长；附录 C.1 待验证） | 新机制：团队 entry 增加 `payee` 字段（须为 members 之一），全员一致指定，不一致 → 队伍作废；中标收益**全额**在下一回合开始时打给 payee（第 6 回合中标则终局前即时到账），队内分配走自由转账，不分是内部信用问题（阶段 2 可用备忘契约指控）。**副作用：本回合危机处罚打不到尚未到账的收益。**行政为个人项目，报酬仍当回合入账 | **2026-08-27 用户：组队提交须注明钱打给谁，达不成一致算失败；成功后钱下一回合打给被指定结算人；他不分出去算他们内部违约。已实现；TDD §4.3/§6.2/附录 C.1 需回写** （已回写 TDD v0.2 / 规则书 V1.0.1）|
| 13 | 2026-08-27 | 规则书 §11.3 准入列 | （裁定引发的规则变更）战争项目原有准入（基础以上 / 组织·核心 / 核心或 2 组织） | 六张战争卡 entry 一律改为无准入；资格加成（§11.1 基础+0/组织+30/核心+60，上限 60）保留 | **2026-08-27 用户：打仗需要人力，人人可参战，不应设准入。已实现；规则书 §11.3 准入列需回写** （已回写 TDD v0.2 / 规则书 V1.0.1）|
| 14 | 2026-08-27 | §5.2 / §5.6 | `QUALIFICATION_GAINED` 契约的 `expiresRound` 与 `byRound` 关系未规定（byRound 已承担"到期作废"职责，expiresRound 是否多余/须相等？） | 登记要求 `expiresRound ≥ byRound`；作废判定以 byRound 为准 | |
| 15 | 2026-08-27 | §5.6 | 契约取消（双方确认，触发前）的时间窗口未规定——冻结阶段取消托管契约会解冻资金，与 §3.2 冻结窗口冲突 | 取消仅限 NEGOTIATION 阶段 | |
| 16 | 2026-08-27 | §8.1 | 多数事件的可见性 TDD 未逐一标注。实现取值：`CONTRACT_REGISTERED`/`MEMO_REGISTERED` 拆 PUBLIC（存在+当事人，MEMO 的 kind 属条款级只进 PARTIES）与 PARTIES（条款）双事件；`CONTRACT_FULFILLED` 金额公开（§8.2 资金流图需要金额）；`CONTRACT_TRIGGERED` PARTIES；`TRANSFER` PARTIES；`INTEL_USED` PUBLIC / `INTEL_REVEALED` SEAT；开局洗牌与情报选字段的 `RNG_DRAWN` 为 HOST（公开会泄漏牌序/字段候选） | 如左 | |
| 17 | 2026-08-27 | §4.5 vs 规则书 §5.3 | `RevealableField` 枚举没有危机卡的 `fundsTarget`/`abilityTarget`/`failPenalty` 与工程卡的 `budgetCap`，但规则书 §5.3 说可揭示「资金要求/能力要求」 | 危机卡的资金/能力目标按语义映射到 `minFunds`/`minAbility` 揭示；`failPenalty` 与 `budgetCap`（规则书清单无对应项）暂不可揭示 | |
| 18 | 2026-08-27 | §5.2 / §6.4 步骤 b | `QUALIFICATION_GAINED` 的语义是「持有该资格」还是「局内新取得」？按「检查资格状态」的行文实现为持有判定——开局就持组织资格的王后/贵族，在下一个 ROUND_START 即可触发以自己为标的的此类契约 | 按持有判定（§6.4「检查步骤 a 后的资格状态」逐字执行） | |
| 19 | 2026-08-27 | 裁定 #10 × §6.2 步骤顺序 | 裁定 #10 的购买锁定 20 在步骤 8（契约执行）时点仍在 lockedFunds 中、契约够不着（§10.2 明确契约只动可用余额）；印章不足时步骤 10 才退回。玩家可借「申请购买」把 20 藏过同回合的结算类契约执行 | 按裁定与流水线顺序的必然结果保留；如需堵住，须人工裁定改结算顺序或购买语义 | |
| 20 | 2026-08-27 | §8.1 | 自动档 / 提名档判定需要「所有申报队伍的名单与各成员投入」「行政申请者及其声明使用的资格」「每人本回合的能力投入与项目收益」，§8.1 现有 PUBLIC 事件都取不到（`PROJECT_RESOLVED` 只公开中标队） | 在结算步骤 11 追加一条 `PROJECT_RESOLVED` / visibility = `HOST` 的汇总事件（`payload.detail = ROUND_FACTS`，见 `packages/engine/src/roundFacts.ts`）。不新增事件类型；选 HOST 而非 PUBLIC 是因为落选队成员的具体出资额、行政申请者的具体能力投入，规则书未规定要向全体公开，且 TDD-001 §8.2 把三张图定位为「引擎 / 主持侧查询」 | |
| 21 | 2026-08-27 | §4.4 / §8.1 / §附录 | TDD-002 的三项变更请求已在引擎实现，TDD-001 正文尚未吸收：CR-1 `MemoContract.relayFrom`；CR-2 危机公开承诺（`Game.crisisPledges` + `CRISIS_PLEDGED` 事件 + `pledgeCrisis` 动作）；CR-3 `StandingRow.overallRank` | 按 TDD-002 §9 实现。另新增两个 TDD-002 自有的事件类型 `BALLOT_OPENED` / `GAME_ENDING_RESOLVED`。TDD-001 应在 v0.3 一并吸收，届时本行清账 | |
| 22 | 2026-08-27 | §8.1 / 阶段 3 | 玩家端要不要显示别人的余额？TDD-001 没规定。线下桌游里资金筹码摆在桌面上是看得见的，但「可用 / 已锁定」一旦拆开显示，等于在提交阶段就把每个人的投入额广播出去 | 玩家端只显示**持有总额**（funds + lockedFunds），不拆分；自己的两项都显示。主持端同样只看总额 | |
| 23 | 2026-08-27 | §4.5 / 阶段 3 | 谈判载体在系统之外（§3.1），因此没有真正的 `ConversationEdge` 来源，而 §9.1 步骤 4 的见证人推导依赖它 | 服务端放了一个最小替身：玩家在磋商阶段自行声明「我在和谁谈」，只用于推导见证人，不进任何结算；对应发出 `CONVERSATION_OPENED` / `CONVERSATION_CLOSED`（PUBLIC）。接入真实载体后替换掉 | |
